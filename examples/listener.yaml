apiVersion: mtjobrunner.io/v1alpha1
kind: MessageListener
metadata:
  name: test-listener
spec:
  topics: ['dk-events','topic2']
  kafka:
    config:
      bootstrap.servers: 10.74.1.187:8080
      group.id: 'events'
